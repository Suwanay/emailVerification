describe('emailVerification', function() {
	beforeEach(module('emailVerification'));

	describe('EmailController', function() {
		let ctrl;

		beforeEach(inject(function($componentController, _$httpBackend_) {}));

		it('Check that the API works', function() {});
	});
});
